# Global Codex Instructions

You are running in WSL on PC-HOME.

Workspace:

- Projects live in `/home/bohdan/code`.
- Agent-ready projects should include `AGENTS.md`, `README.md`, and `docs/architecture.md`.
- OpenCode global config lives in `/home/bohdan/.config/opencode`.

Rules:

- Prefer reading project `AGENTS.md` first.
- Read `docs/architecture.md` before architecture changes.
- Do not push.
- Do not commit unless explicitly requested.
- Ask before destructive commands.
- Do not modify `.env`, credentials, tokens, auth files, or private keys.
- Make small, reviewable changes.
- Summarize changed files and verification steps.
