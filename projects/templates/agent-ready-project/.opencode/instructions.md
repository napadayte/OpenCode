# OpenCode Project Instructions

Follow `AGENTS.md`.

Also read:

- `docs/architecture.md`
- `docs/runbook.md`

Use this workflow:

1. `/analyze` — read-only orientation
2. `/plan` — produce a step-by-step plan
3. Implement manually from the plan
4. `/review` — read-only review before commit

Do not push or commit unless explicitly requested.
